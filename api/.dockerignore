# Kept out of the build context: the image installs its own dependencies, and
# shipping a host-built node_modules would mix native binaries from a different
# platform into an Alpine image.
node_modules
dist
.git
.env
.env.*
npm-debug.log*
*.tsbuildinfo
